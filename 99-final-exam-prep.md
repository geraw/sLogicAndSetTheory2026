---
theme: frankfurt
infoLine: true
author: "גרא וייס"
title: "חזרה לקראת המבחן המסכם"
htmlAttrs:
    dir: rtl
    lang: heb
mdc: true
download: true
highlighter: shiki
transition: slide-left
lineNumbers: false
---

<!-- https://math-wiki.com/index.php/%D7%9E%D7%91%D7%97%D7%A0%D7%99%D7%9D_%D7%91%D7%91%D7%93%D7%99%D7%93%D7%94 -->

# הכנה למבחן המסכם
## מצגת בקורס "מבוא ללוגיקה ותורת הקבוצות"

 מרצה: פרופ. גרא וייס


---

# יחסים ותכונותיהם

**שאלה:** עבור כל אחד מהיחסים הבאים (המוגדרים מעל $\mathcal{P}(\mathbb{N})$ או $\mathcal{P}(\mathbb{N}) \setminus \{\emptyset\}$), קבעו אילו תכונות הוא אילו תכונות הוא מקיים:
* רפלקסיביות,סימטריות,טרנזיטיביות,אנטי-סימטריות

**היחסים:**

1.  מעל $\mathcal{P}(\mathbb{N})$: $A R_1 B \iff A \cap B = \emptyset$ (זרות)
2.  מעל $\mathcal{P}(\mathbb{N}) \setminus \{\emptyset\}$: $A R_2 B \iff \min(A) = \min(B)$ (שוויון מינימום)
3.  מעל $\mathcal{P}(\mathbb{N})$: $A R_3 B \iff |A| \le |B|$ (השוואת עוצמות)
4.  מעל $\mathcal{P}(\mathbb{N}) \setminus \{\emptyset\}$: $A R_4 B \iff \min(A) > \min(B)$ (סדר מינימום חזק)
5.  מעל $\mathcal{P}(\mathbb{N})$: $A R_5 B \iff 1 \in A \cap B$ (חיתוך מכיל 1)
6.  מעל $\mathcal{P}(\mathbb{N})$: $A R_6 B \iff A \subseteq B$ (הכלה)
7.  מעל $\mathcal{P}(\mathbb{N})$: $A R_7 B \iff A \cap B \neq \emptyset$ (חיתוך לא ריק)
8.  מעל $\mathcal{P}(\mathbb{N})$: $A R_8 B \iff A \cup B = \mathbb{N}$ (כיסוי)
9.  מעל $\mathcal{P}(\mathbb{N})$: $A R_9 B \iff A \times B = \emptyset$ (מכפלה קרטזית ריקה)
10. מעל $\mathcal{P}(\mathbb{N})$: $A R_{10} B \iff |A \cap B| < \aleph_0$ (חיתוך סופי)

---

# פתרון: תכונות יחסים (חלק 1)



1.  **$A R_1 B \iff A \cap B = \emptyset$**
    *   **רפלקסיבי:** **לא**. (למשל $A=\{1\}$, $A \cap A \neq \emptyset$).
    *   **סימטרי:** **כן**. ($A \cap B = B \cap A$).
    *   **טרנזיטיבי:** **לא**. ($A=\{1\}, B=\{2\}, C=\{1\}$. $A R_1 B$ ו-$B R_1 C$ אבל לא $A R_1 C$).
    *   **אנטי-סימטרי:** **לא**. ($\{1\} R_1 \{2\}$ ו-$\{2\} R_1 \{1\}$ אך שונים).


2.  **$A R_2 B \iff \min(A) = \min(B)$**
    *   **רפלקסיבי:** **כן**. השוויון הוא יחס שקילות.
    *   **סימטרי:** **כן**.
    *   **טרנזיטיבי:** **כן**.
    *   **אנטי-סימטרי:** **לא**. ($\{1,2\} R_2 \{1,3\}$ אך שונות).
    *   **שקילות:** כן, זהו יחס שקילות.



---

# פתרון: תכונות יחסים (חלק 2)



3.  **$A R_3 B \iff |A| \le |B|$**
    *   **רפלקסיבי:** **כן**.
    *   **סימטרי:** **לא**. (אם גדלים שונים).
    *   **טרנזיטיבי:** **כן**. (מעבר ב-$\le$).
    *   **אנטי-סימטרי:** **לא**. ($\{1\}$ ו-$\{2\}$ שוות עוצמה ויוצרות זוגות הפוכים).

4.  **$A R_4 B \iff \min(A) > \min(B)$**
    *   **רפלקסיבי:** **לא**. ($x > x$ שקר).
    *   **סימטרי:** **לא**.
    *   **טרנזיטיבי:** **כן**.
    *   **אנטי-סימטרי:** **כן** (באופן ריק). לעולם לא יתקיים שניהם, אז התנאי "אם גם וגם אז שווים" מתקיים תמיד.




---

# פתרון: תכונות יחסים (חלק 3)



5.  **$A R_5 B \iff 1 \in A \cap B$**
    *   **רפלקסיבי:** **לא** (עבור קבוצות שלא מכילות את 1). **כן** אם מרחב הדיון היה מוגבל לקבוצות המכילות את 1.
    *   **סימטרי:** **כן**.
    *   **טרנזיטיבי:** **כן**.
        *   אם $A R B$ אז $1 \in A$ ו-$1 \in B$.
        *   אם $B R C$ אז $1 \in B$ ו-$1 \in C$.
        *   מכאן ש-$1 \in A$ ו-$1 \in C$ ולכן $1 \in A \cap C$, כלומר $A R C$.
    *   **אנטי-סימטרי:** **לא**.

6.  **$A R_6 B \iff A \subseteq B$**
    *   **רפלקסיבי:** **כן**.
    *   **סימטרי:** **לא**.
    *   **טרנזיטיבי:** **כן**.
    *   **אנטי-סימטרי:** **כן**. (זהו יחס סדר חלקי קלאסי).



---

# פתרון: תכונות יחסים (חלק 4)



7.  **$A R_7 B \iff A \cap B \neq \emptyset$**
    *   **רפלקסיבי:** **לא**. ($\emptyset \cap \emptyset = \emptyset$). **כן** עבור קבוצות לא ריקות.
    *   **סימטרי:** **כן**.
    *   **טרנזיטיבי:** **לא**. ($A=\{1,2\}, B=\{2,3\}, C=\{3,4\}$ - יש חיתוך, אך $A \cap C = \emptyset$).
    *   **אנטי-סימטרי:** **לא**.

8.  **$A R_8 B \iff A \cup B = \mathbb{N}$**
    *   **רפלקסיבי:** **לא**. ($A \cup A = A \neq \mathbb{N}$ לכל קבוצה חלקית ממש). **כן** רק עבור $A=\mathbb{N}$.
    *   **סימטרי:** **כן**.
    *   **טרנזיטיבי:** **לא**. ($A=\{1\}^c, B=\{1\}, C=\{1\}^c$. $A \cup B = \mathbb{N}, B \cup C = \mathbb{N}$, אך $A \cup C = A \neq \mathbb{N}$).
    *   **אנטי-סימטרי:** **לא**.



---

# פתרון: תכונות יחסים (חלק 5)



9.  **$A R_9 B \iff A \times B = \emptyset$**
    *   **משמעות:** שקול לכך ש-$A=\emptyset$ או $B=\emptyset$.
    *   **רפלקסיבי:** **לא**. ($A \times A \neq \emptyset$ לכל לא ריקה). מתקיים רק לריקה.
    *   **סימטרי:** **כן**. ($A=\emptyset \lor B=\emptyset \iff B=\emptyset \lor A=\emptyset$).
    *   **טרנזיטיבי:** **לא** (בזהירות). אם $A=\{1\}, B=\emptyset, C=\{1\}$. אז $A R B$ ו-$B R C$. אך $A \times C \neq \emptyset$.
    *   **אנטי-סימטרי:** **לא**.

10. **$A R_{10} B \iff |A \cap B| < \aleph_0$**
    *   **רפלקסיבי:** **לא**. (אם $A$ אינסופית, חיתוך עם עצמה הוא אינסופי). **כן** עבור קבוצות סופיות.
    *   **סימטרי:** **כן**.
    *   **טרנזיטיבי:** **לא**. $A=C=\mathbb{N}$, $B=\emptyset$.
    *   **אנטי-סימטרי:** **לא**.

---

# שאלה: יחס שקילות המושרה ע"י פונקציה

**שאלה:**
נגדיר פונקציה $f: \mathbb{N} \to \mathcal{P}(\mathbb{N})$ על ידי הכלל:
$$f(x) = \{n \in \mathbb{N} \mid 2^{n-1} \le x\}$$

נגדיר יחס $T$ על $\mathbb{N}$ על ידי הכלל:
$$x T y \iff f(x) = f(y)$$

**ענו על הבאים:**

א. הוכיחו כי $T$ יחס שקילות על $\mathbb{N}$.

ב. הוכיחו שלכל $n$ טבעי מתקיים:
$$[2^n]_T = \{m \in \mathbb{N} \mid 2^n \le m < 2^{n+1}\}$$
(כאשר $[2^n]_T$ היא מחלקת השקילות של המספר $2^n$).

---

# פתרון: יחס שקילות המושרה ע"י פונקציה

**א. הוכחה ש-$T$ יחס שקילות:**
*   זהו יחס המושרה על ידי שוויון פונקציות (Kernel equivalence).
*   **רפלקסיביות:** לכל $x$, $f(x)=f(x)$ ולכן $xTx$.
*   **סימטריות:** אם $xTy$ אזי $f(x)=f(y)$, אז $f(y)=f(x)$ ולכן $yTx$.
*   **טרנזיטיביות:** אם $xTy$ ו-$yTz$, אזי $f(x)=f(y)$ ו-$f(y)=f(z)$, מכאן $f(x)=f(z)$ ולכן $xTz$.


**ב. חישוב מחלקת השקילות $[2^n]_T$:**
 

<small>
<div class="grid grid-cols-2 gap-4">

<div>

**$\{m \in \mathbb{N} \mid 2^n \le m < 2^{n+1}\} \subseteq [2^n]$:**
*   יהי $m \in \{m \in \mathbb{N} \mid 2^n \le m < 2^{n+1}\}$, כלומר $2^n \le m < 2^{n+1}$.

*   לכל $k \le n+1$: $2^{k-1} \le 2^n \le m$, ולכן $\{0, ..., n+1\} \subseteq f(m)$.
*   לכל $k \ge n+2$: $2^{k-1} \ge 2^{n+1} > m$, ולכן $k \notin f(m)$.
*   קיבלנו $f(m) = \{0, ..., n+1\} = f(2^n)$, ולכן $m \in [2^n]$.

</div>

<div>

**$[2^n] \subseteq \{m \in \mathbb{N} \mid 2^n \le m < 2^{n+1}\}$:**
*   יהי $m \in [2^n]_T$, כלומר $f(m) = f(2^n) = \{0, ..., n+1\}$.

*   בפרט $n+1 \in f(m)$, כלומר $2^{(n+1)-1} \le m \iff 2^n \le m$
*   בנוסף $n+2 \notin f(m)$, כלומר $2^{(n+2)-1} > m \iff 2^{n+1} > m$
*   סה"כ $2^n \le m < 2^{n+1}$, ולכן $m \in \{m \in \mathbb{N} \mid 2^n \le m < 2^{n+1}\}$

</div>

</div>
</small>


---

# שאלה: יחסי סדר על פונקציות

**שאלה:**
תהי $(Y, \le)$ קבוצה סדורה חלקית (פוסֶט). נסמן $A = Y^X$. נגדיר שני יחסים על $A$:

1.  **יחס "לכל"**: $f R_1 g \iff \forall x \in X (f(x) \le g(x))$
2.  **יחס "קיים"**: $f R_2 g \iff \exists x \in X (f(x) \le g(x))$

הוכיחו או הפריכו: היחסים $R_1, R_2$ הם יחסי סדר חלקי על $A$.

---

# פתרון: יחסי סדר על פונקציות 


<small>

**1. יחס "לכל" ($R_1$):** **זהו יחס סדר חלקי.**
*   **רפלקסיביות:** לכל $x$, $f(x) \le f(x)$ ולכן $f R_1 f$.
*   **אנטי-סימטריות:** אם $\forall x (f(x) \le g(x))$ ו-$\forall x (g(x) \le f(x))$, אז מאנטי-סימטריות ב-$Y$, לכל $x$ מתקיים $f(x)=g(x)$, כלומר $f=g$.
*   **טרנזיטיביות:** נניח $f R_1 g$ ו-$g R_1 h$.
    *   אזי לכל $x$: $f(x) \le g(x)$ וגם $g(x) \le h(x)$.
    *   מטרנזיטיביות ב-$Y$ נקבל שלכל $x$: $f(x) \le h(x)$.
    *   לכן $f R_1 h$.

**2. יחס "קיים" ($R_2$):** **אינו יחס סדר חלקי.**
*   **רפלקסיביות:** מתקיימת (כי קיים $x$ כלשהו, למשל אם $X \neq \emptyset$).
<div class="grid grid-cols-2 gap-4">

<div>

*   **אנטי-סימטריות:** **לא מתקיימת.**
    *   דוגמא נגדית: $X=\{1,2\}, Y=\mathbb{N}$, $f(1)=g(2)=2$, $f(2)=g(1)=1$.
    *   $f(1) \le g(1)$ ו-$g(2) \le f(2)$ ולכן $f R_2 g$ ו-$g R_2 f$, אך $f \neq g$.

</div>

<div>

*   **טרנזיטיביות:** **לא מתקיימת.**
    *   **דוגמא נגדית:** $X=\{1,2\}, Y=\mathbb{N}$.
    *   $f(1) = f(2) = 2$,  $g(1) = 3, g(2) = 1$, $h(1) = 1, h(2) = 1$
    *   $f R_2 g$ (אינדקס 1: $f(1) \le g(1)$).
    *   $g R_2 h$ (אינדקס 2: $g(2) \le h(2)$).
    *   **אבל** לא $f R_2 h$: $f(1) > h(1)$ ו-$f(2) > h(2)$.

</div>

</div>

</small>
    
<div class="absolute bottom-50 left-10">
    <img src="/function_counterexample.png" class="h-45" />
</div>

    


---


# שאלה: יחס סדר על פונקציות

**שאלה:**
נגדיר יחס על $A = \{0, 1, 2\}^\mathbb{N}$ כך: $f R g$ אמ"מ
$$ (f=g) \lor (\forall n \in \mathbb{N}( f(n) = f(n)g(n)) ) $$

**ענו על הבאים:**

א. הוכיחו ש-$R$ יחס סדר על $A$.

ב. מצאו את כל האיברים המקסימליים והאיברים המינימליים בקבוצה $A$ עם היחס $R$.

ג. הוכיחו/הפריכו: היחס $R$ הוא יחס סדר לינארי/קווי על $A$.



---

# פתרון: יחס סדר על פונקציות

<div class="absolute top-80 left-60 p-4">

<table style="font-size: 0.8rem; width: auto; min-width: 30%; margin: 0 auto;">
<thead>
<tr>
<th style="padding: 2px px;"> 

$x$ \ $y$
</th>
<th style="padding: 2px 8px;">0</th>
<th style="padding: 2px 8px;">1</th>
<th style="padding: 2px 8px;">2</th>
</tr>
</thead>
<tbody>
<tr>
<td style="font-weight: bold; padding: 2px 8px;">0</td>
<td style="padding: 2px 8px;">✅</td>
<td style="padding: 2px 8px;">✅</td>
<td style="padding: 2px 8px;">✅</td>
</tr>
<tr>
<td style="font-weight: bold; padding: 2px 8px;">1</td>
<td style="padding: 2px 8px;">❌</td>
<td style="padding: 2px 8px;">✅</td>
<td style="padding: 2px 8px;">❌</td>
</tr>
<tr>
<td style="font-weight: bold; padding: 2px 8px;">2</td>
<td style="padding: 2px 8px;">❌</td>
<td style="padding: 2px 8px;">✅</td>
<td style="padding: 2px 8px;">✅</td>
</tr>
</tbody>
</table> 
</div>

<small>

<!-- **ניתוח התנאי:**
התנאי $x = x \cdot y$ עבור $x,y \in \{0,1,2\}$ מגדיר את סדר הרכיבים:
*   עבור $x=0$: $0 = 0 \cdot y$ תמיד אמת. לכן $0 \le 0, 1, 2$.
*   עבור $x=2$: $2 = 2 \cdot y \implies y=1$. לכן $2 \le 1$. (שימו לב: $2 \not\le 2$ עפ"י הנוסחה בלבד, אך $f=g$ מכסה זאת).
*   עבור $x=1$: $1 = 1 \cdot y \implies y=1$. לכן $1 \le 1$.
*   **הסדר ברכיבים:** $0 < 2 < 1$. -->



**א. הוכחה ש-$R$ יחס סדר:**
*   **רפלקסיביות:** נתון בהגדרה ($f=g$).
*   **אנטי-סימטריות:** נניח $f R g$ ו-$g R f$. אם $f=g$ סיימנו. אחרת, $\forall n (f(n)=f(n)g(n))$ וגם $g(n)=g(n)f(n)$. מכיוון שהכפל חילופי, $f(n) = f(n)g(n) = g(n)f(n) = g(n)$, לכן $f=g$.
*   **טרנזיטיביות:** נניח $f R g$ ו-$g R h$. אם אחד מהזוגות זהים, סיימנו. אחרת, $\forall n (f(n)=f(n)g(n))$ וגם $g(n)=g(n)h(n)$. ע"י הצבה ואסוציאטיביות הכפל: $f(n) = f(n)g(n) = f(n)(g(n)h(n)) = (f(n)g(n))h(n) = f(n)h(n)$, כלומר $f R h$.

**ב. מינימום ומקסימום:**
*   **מינימום:** הפונקציה הקבועה $\vec{0}$ (כי $0$ הוא המינימום ברכיבים). זוהי המינימלית היחידה.
*   **מקסימום:** הפונקציה הקבועה $\vec{1}$ (כי $1$ הוא המקסימום ברכיבים). זוהי המקסימלית היחידה.

**ג. לינאריות:**
*   **הפרכה:** $R$ אינו לינארי.
*   ניקח $f=(2, 0, 0, \dots)$ ו-$g=(0, 2, 0, \dots)$.
*   $f(0)=2 > 0=g(0)$ (לפי סדר הרכיבים), לכן לא ייתכן $f R g$.
*   $g(1)=2 > 0=f(1)$ (לפי סדר הרכיבים), לכן לא ייתכן $g R f$.


</small>




---

# שאלה: סדר על פונקציות לקבוצות

**שאלה:**
תהי $A = \mathcal{P}(\mathbb{N})^\mathbb{N}$ קבוצת הפונקציות מ-$\mathbb{N}$ ל-$\mathcal{P}(\mathbb{N})$.
נגדיר יחס $R$ על $A$ על ידי: $f R g \iff \forall n \in \mathbb{N} (f(n) \subseteq g(n))$.


**א.** הוכיחו כי $R$ הינו יחס סדר חלקי.

**ב.** האם $R$ יחס מלא (לינארי)? הוכיחו.

**ג.** האם יש איבר גדול ביותר ב-$A$? אם כן, מצאו אותו והוכיחו שהוא הגדול ביותר. אם לא, הוכיחו שאין איבר גדול ביותר.

**ד.** תהי $X = \{ f \in A \mid \forall n \in \mathbb{N} (n \in f(n)) \}$.

האם יש איבר קטן ביותר ב-$X$? (לפי היחס $R$). אם כן, מצאו והוכיחו. אם לא, הוכיחו.

**ה.** תהי $D = \{ f \in A \mid \forall n \in \mathbb{N} (f(n) \subseteq f(n+1) \land f(n) \neq f(n+1)) \}$.
    האם יש איבר קטן ביותר ב-$D$? אם כן, מצאו והוכיחו. אם לא, הוכיחו.

---

# פתרון: סדר על פונקציות לקבוצות (חלק 1)

<div class="grid grid-cols-2 gap-4">

<div>

**א. הוכחה ש-$R$ יחס סדר חלקי:**
*   **רפלקסיביות:** לכל $n$, $f(n) \subseteq f(n)$, לכן $f R f$.
*   **אנטי-סימטריות:** נניח $f R g$ ו-$g R f$.
    *   אזי לכל $n$, $f(n) \subseteq g(n)$ וגם $g(n) \subseteq f(n)$.
    *   מכאן שלכל $n$, $f(n) = g(n)$.
    *   לכן $f=g$.
*   **טרנזיטיביות:** נניח $f R g$ ו-$g R h$.
    *   אזי לכל $n$, $f(n) \subseteq g(n)$ וגם $g(n) \subseteq h(n)$.
    *   טרנזיטיביות ההכלה גוררת $f(n) \subseteq h(n)$.
    *   לכן $f R h$.

</div>

<div>

**ב. אינו יחס מלא:**
*   נפריך ע"י דוגמה נגדית:
*   נגדיר $f(n) = \{1\}$ ו-$g(n) = \{2\}$ לכל $n$.
*   $f(0)=\{1\} \not\subseteq \{2\}=g(0)$, לכן לא $f R g$.
*   $g(0)=\{2\} \not\subseteq \{1\}=f(0)$, לכן לא $g R f$.

</div>

</div>

---

# פתרון: סדר על פונקציות לקבוצות (חלק 2)

<div class="grid grid-cols-2 gap-4">

<div>

**ג. איבר גדול ביותר ב-$A$:**
*   **קיים.** האיבר הוא הפונקציה הקבועה $M(n) = \mathbb{N}$ לכל $n$.
*   **הוכחה:** תהי $f \in A$ כלשהי.
*   לכל $n$, $f(n) \in \mathcal{P}(\mathbb{N})$ ולכן $f(n) \subseteq \mathbb{N}$.
*   כלומר $f(n) \subseteq M(n)$ לכל $n$.
*   לכן $f R M$ לכל $f$, ו-$M$ הוא האיבר הגדול ביותר.

</div>

<div>

**ד. איבר קטן ביותר ב-$X$:**
*   הקבוצה $X$ מכילה את הפונוקציות ש-$n$ נמצא ב-$f(n)$.
*   **קיים.** האיבר הוא $m(n) = \{n\}$.
*   **הוכחה:**
    1.  נראה ש-$m \in X$: לכל $n$, האם $n \in m(n)$? כן, כי $m(n)=\{n\}$.
    2.  נראה שהוא מינימום: תהי $f \in X$ כלשהי.
    3.  לפי הגדרת $X$, לכל $n$ מתקיים $n \in f(n)$.
    4.  לכן $\{n\} \subseteq f(n)$, כלומר $m(n) \subseteq f(n)$.
    5.  סה"כ $m R f$ לכל $f \in X$.

</div>

</div>

---

# פתרון: סדר על פונקציות לקבוצות (חלק 3)

**ה. איבר קטן ביותר ב-$D$:**
*   הקבוצה $D$ מכילה פונקציות שהן סדרות עולות ממש של קבוצות.
*   **לא קיים.**
*   **הוכחה:** נניח בשלילה שקיים איבר קטן ביותר $m \in D$.
*   אזי לכל $f \in D$, מתקיים $m R f$, כלומר לכל $n$, $m(n) \subseteq f(n)$.
*   בפרט, $m(n) \subseteq \bigcap \{ f(n) \mid f \in D \}$.
*   נבנה משפחה של פונקציות ב-$D$: לכל $k \in \mathbb{N}$, נגדיר $f_k(n) = \{k, k+1, \dots, k+n\}$.
    *   בדיקה ש-$f_k \in D$: $|f_k(n)| = n+1$ ו-$|f_k(n+1)| = n+2$, ו- $f_k(n) \subseteq f_k(n+1)$, לכן עולה ממש.
*   כעת נחשב את החיתוך עבור $n$ נתון: לכל $x \in \mathbb{N}$, נבחר $k > x$. אזי $x \notin \{k, \dots, k+n\} = f_k(n)$.
*   לכן $x$ לא נמצא בחיתוך של כל ה-$f_k(n)$. החיתוך הוא ריק.
*   משמע שחייב להתקיים $m(n) \subseteq \emptyset$, כלומר $m(n) = \emptyset$ לכל $n$.
*   אבל אם $m(n) = \emptyset$ לכל $n$, אזי $m(n) = m(n+1)$, ולכן $m \notin D$ (סתירה לתנאי העלייה הממש).
*   **מסקנה:** לא קיים איבר קטן ביותר ב-$D$.

---

# שאלה: הרכבת פונקציות

* תהיינה $g: A \to B, f: B \to C$ פונקציות.
    
    * הוכיחו שאם $f \circ g$ חח"ע, אז $g$ חח"ע.
    
    * הוכיחו שאם $f \circ g$ על, אז $f$ היא על.

* תהי $f: A \to B$ פונקציה.
    
    * נגדיר ש-$f$ **מצטמצמת משמאל** אם לכל קבוצה $C$ ולכל שתי פונקציות $g,h: C \to A$, אם $f \circ g = f \circ h$ אזי $g=h$.
    
    * הוכיחו ש-$f$ מצטמצמת משמאל אם ורק אם $f$ חח"ע.

---

# פתרון: הרכבת פונקציות


<small>

<div class="grid grid-cols-2 gap-4">


<div>

**א. תכונות בסיסיות:**

i. **נתון:** $f \circ g$ חח"ע.
*   **צ"ל:** $g$ חח"ע.
*   **הוכחה:** יהיו $x_1, x_2 \in A$ כך ש-$g(x_1) = g(x_2)$.
*   נפעיל את $f$ על שני האגפים: $f(g(x_1)) = f(g(x_2))$.
*   לפי הגדרת ההרכבה: $(f \circ g)(x_1) = (f \circ g)(x_2)$.
*   היות ו-$f \circ g$ חח"ע, נובע ש-$x_1 = x_2$.
*   לכן $g$ חח"ע.

ii. **נתון:** $f \circ g$ על.
*   **צ"ל:** $f$ על.
*   **הוכחה:** יהי $c \in C$.
*   היות ו-$f \circ g$ על, קיים $a \in A$ כך ש-$(f \circ g)(a) = c$.
*   לפי הגדרת ההרכבה: $f(g(a)) = c$.
*   נסמן $b = g(a) \in B$. קיבלנו $f(b) = c$.
*   לכן לכל $c \in C$ קיים מקור ב-$B$, כלומר $f$ על.

</div>

<div>

**ב. צמצום משמאל $\iff$ חח"ע:**

$\Rightarrow$ **כיוון ראשון:** נניח $f$ חח"ע.
*   נניח $f \circ g = f \circ h$.
*   יהי $c \in C$ כלשהו. אזי $f(g(c)) = f(h(c))$.
*   מכיוון ש-$f$ חח"ע, נובע ש-$g(c) = h(c)$.
*   זה נכון לכל $c$, לכן $g=h$.

$\Leftarrow$ **כיוון שני:** נניח $f$ מצטמצמת משמאל.
*   נוכיח ש-$f$ חח"ע.
*   יהיו $x, y \in A$ כך ש-$f(x) = f(y)$.
*   נגדיר קבוצה $C = \{ k \}$ (יחידון), ופונקציות $g, h: C \to A$ ע"י $g(k) = x, h(k) = y$
*   מתקיים: $(f \circ g)(k) = f(x) = f(y) = (f \circ h)(k)$.
*   לכן $f \circ g = f \circ h$.
*   לפי הנתון (צמצום משמאל), נובע $g = h$.
*   לכן בפרט $g(k) = h(k)$, כלומר $x = y$.
*   הוכחנו שאם $f(x)=f(y)$ אז $x=y$, כלומר $f$ חח"ע.

</div>

</div>

</small>

---

# שאלה: עוצמות


*   קבעו והוכיחו לכל קבוצה אם היא מעוצמת $2^{\aleph_0}, \aleph_0$ או סופית. אם היא סופית רשמו את מספר האיברים בקבוצה.
    *   $A = \mathbb{Q}^{\mathbb{N}}$
    *   $B \subseteq \{0,1\}^{\mathbb{N}}$ אוסף הפונקציות **העל** מהטבעיים לקבוצה $\{0,1\}$.
    *   $C \subseteq \mathcal{P}(\mathbb{N})^{\mathbb{Q}}$ אוסף הפונקציות **ההפיכות** מהרציונליים לקבוצת החזקה של הטבעיים.

*   תהי $D \subseteq \mathbb{N}^{\mathbb{N}}$ אוסף הפונקציות **החח"ע** מהטבעיים לטבעיים.
    *   מצאו פונקציה חח"ע $h: \{0,1\}^{\mathbb{N}} \to D$ והוכיחו שהיא אכן חח"ע.
    *  הוכיחו ש-$|D| = \aleph$.

---

# פתרון: עוצמות (חלק 1)

<small>

<div class="grid grid-cols-2 gap-4">

<div>

**א. חישוב עוצמות:**

i. **הקבוצה $A = \mathbb{Q}^{\mathbb{N}}$:**
*   מתקיים $|A| = |\mathbb{Q}|^{|\mathbb{N}|} = \aleph_0^{\aleph_0}$.
*   מכיוון ש-$2 \le \aleph_0 \le 2^{\aleph_0}$, לפי חוקי חזקות:
    $$ 2^{\aleph_0} \le \aleph_0^{\aleph_0} \le (2^{\aleph_0})^{\aleph_0} = 2^{\aleph_0 \cdot \aleph_0} = 2^{\aleph_0} $$
*   לפי משפט קנטור-ברנשטיין, $|A| = 2^{\aleph_0} = \aleph$.

ii. **הקבוצה $B$ (פונקציות על):**
*   $B \subseteq \{0,1\}^{\mathbb{N}}$, ולכן $|B| \le 2^{\aleph_0}$.
*   מצד שני, יש רק 2 פונקציות ש**אינן** על: הפונקציה הקבועה 0 והפונקציה הקבועה 1.
*   לכן $|B| = 2^{\aleph_0} - 2 = 2^{\aleph_0} = \aleph$.
*   (הערה: הורדת מספר סופי מקבוצה אינסופית לא משנה את עוצמתה).

</div>

<div>

iii. **הקבוצה $C$ (פונקציות הפיכות):**
*   פונקציה הפיכה היא פונקציה חח"ע ועל.
*   קיימת כזו רק אם התחום והטווח שווי עוצמה.
*   התחום: $|\mathbb{Q}| = \aleph_0$.
*   הטווח: $|\mathcal{P}(\mathbb{N})| = 2^{\aleph_0} = \aleph$.
*   העוצמות שונות ($\aleph_0 \neq \aleph$), ולכן לא קיימות פונקציות הפיכות ביניהן.
*   הקבוצה $C$ היא הקבוצה הריקה.
*   $|C| = 0$ (קבוצה סופית).

</div>

</div>

</small>

---

# פתרון: עוצמות (חלק 2)

<small>

<div class="grid grid-cols-2 gap-4">

<div>

**ב. הפונקציה החח"ע והעוצמה של $D$:**

i. **בניית $h: \{0,1\}^{\mathbb{N}} \to D$:**
*   לכל סדרה בינארית $f \in \{0,1\}^{\mathbb{N}}$, נגדיר פונקציה $g_f \in D$ (פונקציה מ-$\mathbb{N}$ ל-$\mathbb{N}$ שהיא חח"ע).
*   רעיון: נמפה את $n$ למספר שמקודד גם את $n$ וגם את הערך $f(n)$. למשל $g_f(n) = 2n + f(n)$.
*   מכיוון ש-$f(n) \in \{0,1\}$, המספר $2n+f(n)$ הוא פשוט $2n$ או $2n+1$.
*   **בדיקה ש-$g_f$ חח"ע:** נניח $g_f(n) = g_f(m)$.
    *   אזי $2n + f(n) = 2m + f(m)$.
    *   מאחר ש-$f(n), f(m) \in \{0,1\}$, הזוגיות של שני האגפים נקבעת ע"י $f(n)$ ו-$f(m)$. לכן $f(n)=f(m)$.
    *   נחסר אותם ונקבל $2n=2m \implies n=m$. לכן $g_f \in D$.

</div>

<div>

*   **בדיקה ש-$h$ ($f \mapsto g_f$) חח"ע:**
    *   נניח $h(f_1) = h(f_2)$, כלומר הפונקציות $g_{f_1}$ ו-$g_{f_2}$ זהות.
    *   לכל $n$, $g_{f_1}(n) = g_{f_2}(n)$.
    *   כלומר $2n + f_1(n) = 2n + f_2(n)$.
    *   נחסר $2n$ ונקבל $f_1(n) = f_2(n)$ לכל $n$.
    *   לכן $f_1 = f_2$.

ii. **הוכחת $|D| = \aleph$:**
*   מצד אחד, $D \subseteq \mathbb{N}^{\mathbb{N}}$, ולכן $|D| \le |\mathbb{N}^{\mathbb{N}}| = \aleph_0^{\aleph_0} = 2^{\aleph_0} = \aleph$.
*   מצד שני, בנינו פונקציה חח"ע מ-$\{0,1\}^{\mathbb{N}}$ ל-$D$.
*   לכן $|D| \ge |\{0,1\}^{\mathbb{N}}| = 2^{\aleph_0} = \aleph$.
*   לפי משפט קנטור-ברנשטיין, $|D| = \aleph$.

</div>

</div>

</small>

---

# שאלה: יחסים וסדר על פונקציות (מורחב)

**הגדרות:**
*   נסמן $A(f,g)=\{n \mid n \in \mathbb{N} \land f(n)>g(n)\}$.
*   נגדיר יחס $R$ על $\mathbb{N}^{\mathbb{N}}$: $R = \{ \langle f, g \rangle \mid f,g \in \mathbb{N}^{\mathbb{N}} \land A(f,g) \in Fin(\mathbb{N}) \}$.
*   נסמן $D(f,g)=\{ n \mid n \in \mathbb{N} \land f(n) \neq g(n) \}$.
*   נגדיר יחס $R^* = \{ \langle f,g \rangle \mid f,g \in \mathbb{N}^{\mathbb{N}} \land D(f,g) \in Fin(\mathbb{N}) \}$.

**סעיפים:**
*   **(א)** הוכיחו כי $R$ קדם-סדר מעל $\mathbb{N}^{\mathbb{N}}$ וגם $R$ אינו יס"ח מעל $\mathbb{N}^{\mathbb{N}}$ וגם $R$ אינו יחס שקילות.
*   **(ב)** הוכיחו כי לכל $f \in \mathbb{N}^{\mathbb{N}}$, מתקיים $R[\{f\}] \sim \mathcal{P}(\mathbb{N})$.
*   **(ד)** הוכיחו כי הקבוצה $\bigcup \{ \mathbb{N}^A \mid A \in Fin(\mathbb{N}) \}$ היא בת מנייה.
*   **(ה)** הוכיחו כי לכל $f \in \mathbb{N}^{\mathbb{N}}$, מתקיים $[f]_{R^*} \sim \mathbb{N}$.
*   **(ו)** הוכיחו כי קבוצת המנה $\mathbb{N}^{\mathbb{N}} / R^*$ אינה בת מנייה.
*   **(ז)** הוכיחו כי לכל $f,g \in \mathbb{N}^{\mathbb{N}}$, $[g]_{R^*}$ אינה עוקב מיידי של $[f]_{R^*}$ לפי היחס $\underline{R}_{R^*}$.

---

# פתרון: סעיף א' - תכונות היחס $R$

<small>

**1. $R$ הוא קדם-סדר (רפלקסיבי וטרנזיטיבי):**
*   **רפלקסיביות:** לכל $f$, $A(f,f) = \{n \mid f(n)>f(n)\} = \emptyset$. הקבוצה הריקה היא סופית, לכן $f R f$.
*   **טרנזיטיביות:** נניח $f R g$ ו-$g R h$.
    *   אזי הקבוצות $K_1 = \{n \mid f(n)>g(n)\}$ ו-$K_2 = \{n \mid g(n)>h(n)\}$ סופיות.
    *   נשים לב ש: $f(n)>h(n) \implies (f(n)>g(n)) \lor (g(n)>h(n))$.
        *   הסבר: נניח בשלילה שגם $f(n) \le g(n)$ וגם $g(n) \le h(n)$.
        *   מטרנזיטיביות הסדר בטבעיים נקבל $f(n) \le h(n)$, וזו סתירה להנחה ש-$f(n) > h(n)$.
    *   כלומר $A(f,h) \subseteq K_1 \cup K_2$.
    *   איחוד קבוצות סופיות הוא סופי, לכן $A(f,h)$ סופית, ולכן $f R h$.

<div class="grid grid-cols-2 gap-4">

<div>

**2. $R$ אינו סימטרי (אינו יחס שקילות):**
*   ניקח $f(n)=1$ ו-$g(n)=2$ לכל $n$.
*   $f(n)$ תמיד קטן מ-$g(n)$ אז $A(f,g)=\emptyset$ (סופי) $\Leftarrow$ $f R g$.
*   $g(n)$ תמיד גדול מ-$f(n)$ אז $A(g,f)=\mathbb{N}$ (אינסופי) $\Leftarrow$ לא $g R f$. 
* זה מראה שהיחס אינו סימטרי ולכן לא יחס שקילות


</div>

<div>

**3. $R$ אינו אנטי-סימטרי (לא יחס סדר חלקי)**
    
* נגדיר $f(n)=1, g(n)=1$ עבור $n>0$, ו-$f(0)=1, g(0)=2$.
*   $A(f,g)=\emptyset \implies f R g$.
*   $A(g,f)=\{0\} \in Fin(\mathbb{N}) \implies g R f$.
*   אבל $f \neq g$.



</div>

</div>

</small>

---

# פתרון: סעיף ב' - עוצמת $R[\{f\}]$

**הטענה:** לכל $f \in \mathbb{N}^{\mathbb{N}}$, הקבוצה $R[\{f\}] = \{ g \mid f R g \}$ שוות עוצמה ל-$\mathcal{P}(\mathbb{N})$ (כלומר $\aleph$).

**הוכחה:**
*   כידוע $|\mathcal{P}(\mathbb{N})| = 2^{\aleph_0} = \aleph$ ו-$|\mathbb{N}^{\mathbb{N}}| = \aleph$.
*   ברור ש-$R[\{f\}] \subseteq \mathbb{N}^{\mathbb{N}}$, לכן $|R[\{f\}]| \le \aleph$.
*   נראה ש-$|R[\{f\}]| \ge \aleph$.
*   לכל $h \in \mathbb{N}^{\mathbb{N}}$, נגדיר $g_h = f + h$ (כלומר $g_h(n) = f(n) + h(n)$).
*   לכל $n$, בפרט $g_h(n) \ge f(n)$, ולכן הקבוצה $\{n \mid f(n) > g_h(n)\}$ היא ריקה (וסופית).
*   לכן לכל $h$, מתקיים $f R g_h$, כלומר $g_h \in R[\{f\}]$.
*   ההתאמה $h \mapsto g_h$ היא חח"ע (כי אם $f+h_1 = f+h_2$ אז $h_1=h_2$).
*   לכן יש לפחות $|\mathbb{N}^{\mathbb{N}}| = \aleph$ פונקציות ב-$R[\{f\}]$.
*   ממשפט קנטור-ברנשטיין, העוצמה היא $\aleph$.

---

# פתרון: סעיף ד' - איחוד קבוצות פונקציות סופיות

**הטענה:** הקבוצה $S = \bigcup \{ \mathbb{N}^A \mid A \in Fin(\mathbb{N}) \}$ היא בת מנייה ($\aleph_0$).

**הוכחה:**
*   הקבוצה $Fin(\mathbb{N})$ (כל תתי הקבוצות הסופיות של הטבעיים) היא בת מנייה. (ידוע).
*   נבדוק את העוצמה של כל איבר באיחוד, $\mathbb{N}^A$.
*   אם $A$ קבוצה סופית, נאמר $|A|=k$, אזי העוצמה של $\mathbb{N}^A$ (קבוצת הפונקציות מ-$A$ ל-$\mathbb{N}$) היא $|\mathbb{N}|^{|A|} = \aleph_0^k = \aleph_0$.
*   כלומר, כל קבוצה באיחוד היא בת מנייה.
*   קיבלנו איחוד בן-מנייה של קבוצות בנות-מנייה.
*   לפי משפט האיחוד ("איחוד בן מנייה של קבוצות בנות מנייה הוא בן מנייה"), העוצמה של $S$ היא $\aleph_0$.

---

# פתרון: סעיף ה' - עוצמת מחלקת השקילות $[f]_{R^*}$

**הטענה:** לכל $f \in \mathbb{N}^{\mathbb{N}}$, מתקיים $[f]_{R^*} \sim \mathbb{N}$.

**הוכחה:**
*   לפי הגדרה, $g \in [f]_{R^*} \iff D(f,g) \in Fin(\mathbb{N})$.
*   כלומר, $g$ מתקבלת מ-$f$ ע"י שינוי ערכים במספר סופי של נקודות.
*   נגדיר פונקציה מהקבוצה $S$ (מסעיף ד') ל-$[f]_{R^*}$.
*   לכל פונקציה חלקית סופית $h \in \mathbb{N}^A$ (כאשר $A \in Fin(\mathbb{N})$), נגדיר $\hat{h} \in \mathbb{N}^{\mathbb{N}}$:
    $$ \hat{h}(n) = \begin{cases} h(n) & n \in A \\ f(n) & n \notin A \end{cases} $$
*   ברור ש-$\hat{h}$ שונה מ-$f$ רק בתוך $A$ (קבוצה סופית), לכן $\hat{h} \in [f]_{R^*}$.
*   ההתאמה היא "על" $[f]_{R^*}$: כל $g$ שקולה ל-$f$ שונה ממנה בקבוצה סופית $A$, ונגדיר $h$ כצמצום של $g$ ל-$A$.
*   ראינו ש-$|S| = \aleph_0$. לכן $|[f]_{R^*}| \le \aleph_0$.
*   מצד שני, יש אינסוף פונקציות השונות מ-$f$ בנקודה אחת בלבד (למשל $f(0)+k$), לכן $|[f]_{R^*}| \ge \aleph_0$.
*   סה"כ העוצמה היא $\aleph_0$.

---

# פתרון: סעיף ו' - עוצמת קבוצת המנה

**הטענה:** קבוצת המנה $\mathbb{N}^{\mathbb{N}} / R^*$ אינה בת מנייה.

**הוכחה:**
*   נסמן $X = \mathbb{N}^{\mathbb{N}}$. העוצמה היא $|X| = \aleph$.
*   קבוצת המנה מחלקת את $X$ למחלקות זרות שהאיחוד שלהן הוא $X$.
    $$ X = \bigcup_{C \in X/R^*} C $$
*   הוכחנו בסעיף ה' שכל מחלקה $C$ היא בת מנייה ($|C| = \aleph_0$).
*   נניח בשלילה שקבוצת המנה בת מנייה. אזי האיחוד הנ"ל הוא איחוד בן מנייה של קבוצות בנות מנייה.
*   לכן העוצמה של האיחוד צריכה להיות $\aleph_0$.
*   אבל העוצמה של האיחוד היא $|X| = \aleph$ (שהיא גדולה מ-$\aleph_0$).
*   סתירה.
*   **מסקנה:** קבוצת המנה אינה בת מנייה (למעשה עוצמתה $\aleph$).

---

# פתרון: סעיף ז' - צפיפות היחס

**הטענה:** לכל $[f],[g]$ כך ש-$[f] < [g]$ (ביחס המושרה), $[g]$ אינה עוקב מיידי של $[f]$.
**משמעות:** קיים $h$ כך ש-$[f] < [h] < [g]$.

<!-- 
היחס המושרה $[f] \le [g]$ משמעותו $f R g$ (כמעט תמיד $f \le g$).
$[f] < [g]$ אומר ש-$f \le g$ כמעט תמיד, אבל הקבוצה $\{ n \mid g(n) > f(n) \}$ היא אינסופית. -->

*   תהי $P = \{ n \in \mathbb{N} \mid g(n) > f(n) \}$. נתון ש-$P$ אינסופית.
*   נחלק את $P$ לשתי קבוצות אינסופיות זרות $P_1, P_2$ (למשל, זוגיים ואי זוגיים בתוך $P$).
*   נגדיר פונקציה $h$:
    $$ h(n) = \begin{cases} g(n) & n \in P_1 \\ f(n) & n \notin P_1 \end{cases} $$
*   **נראה ש-$[f] < [h]$:**
    *   $f(n) \le h(n)$ לכל $n$ (כי ב-$P_1$, $f < g = h$, ובחוץ $f=h$). לכן $[f] \le [h]$.
    *   הקבוצה $\{ n \mid h(n) > f(n) \}$ היא $P_1$ (אינסופית), לכן לא מתקיים $h R f$. כלומר $[f] \neq [h]$.
*   **נראה ש-$[h] < [g]$:**
    *   $h(n) \le g(n)$ לכל $n$ (ב-$P_1$ שווה, בחוץ $h=f \le g$). לכן $[h] \le [g]$.
    *   הקבוצה $\{ n \mid g(n) > h(n) \}$ היא $P_2$ (אינסופית), לכן לא $[g] \le [h]$.
*   סה"כ $[f] < [h] < [g]$. לא קיים עוקב מיידי.
