---
theme: frankfurt
infoLine: true
author: "גרא וייס"
title: "חזרה לקראת הבוחן השני"
htmlAttrs:
   dir: rtl
   lang: heb
mdc: true
download: true
highlighter: shiki
transition: slide-left
lineNumbers: false
---

<!-- https://math-wiki.com/index.php/%D7%9E%D7%91%D7%97%D7%A0%D7%99%D7%9D_%D7%91%D7%91%D7%93%D7%99%D7%93%D7%94 -->

# הכנה לבוחן השני
## מצגת בקורס "מבוא ללוגיקה ותורת הקבוצות"

 מרצה: פרופ. גרא וייס


---
section: הגדרות
---

# רשימת הגדרות שחובה לזכור בע"פ


*   **יחס רפלקסיבי על $A$:** לכל $a \in A$, מתקיים $aRa$.
*   **יחס סימטרי:** לכל $a,b \in A$, אם $aRb$ אז $bRa$.
*   **יחס אנטי-סימטרי:** לכל $a,b \in A$, אם $aRb$ וגם $bRa$, אז $a=b$.
*   **יחס טרנזיטיבי:** לכל $a,b,c \in A$, אם $aRb$ וגם $bRc$, אז $aRc$.
*   **יחס סדר חלקי (קס"ח):** יחס שהוא רפלקסיבי, אנטי-סימטרי וטרנזיטיבי.
*   **יחס שקילות:** יחס שהוא רפלקסיבי, סימטרי וטרנזיטיבי.
*   **פונקציה חח"ע (Injective):** לכל $x_1, x_2$, אם $f(x_1) = f(x_2)$ אז $x_1 = x_2$.
*   **פונקציה על (Surjective):** לכל $y \in B$ קיים $x \in A$ כך ש-$f(x)=y$.
*   **תמונה (Image):** $f[A'] = \{ f(x) \mid x \in A' \}$.
*   **קדם-תמונה (Pre-image):** $f^{-1}[B'] = \{ x \in A \mid f(x) \in B' \}$.
*   **קבוצה סופית:** קבוצה $A$ היא סופית אם קיים $n \in \mathbb{N}$ ופונקציה חח"ע ועל $f: A \to \mathbb{N}^{<n}$ (כאשר $\mathbb{N}^{<n} = \{0, ..., n-1\}$).


---
section: תכונות יחסים
---

# סגירות תחת פעולות קבוצתיות
האם התכונה נשמרת כאשר מבצעים פעולה על שני יחסים המקיימים אותה?

| תכונה | איחוד ($\cup$) | חיתוך ($\cap$) | הפרש סימטרי ($\Delta$) |
| :--- | :---: | :---: | :---: |
| **רפלקסיביות** | ✅ | ✅ | ❌ |
| **סימטריות** | ✅ | ✅ | ✅ | 
| **אנטי-סימטריות** | ❌ | ✅ | ❌ |
| **טרנזיטיביות** | ❌ | ✅ | ❌ |

---

# הסבר: רפלקסיביות
*   **איחוד ($\cup$) - ✅:**
    *   כיוון ש-$R$ רפלקסיבי, מתקיים $I_A \subseteq R$.
    *   לכן $I_A \subseteq R \cup S$.

*   **חיתוך ($\cap$) - ✅:**
    *   כיוון ש-$R, S$ רפלקסיביים, מתקיים $I_A \subseteq R$ וגם $I_A \subseteq S$.
    *   לכן $I_A \subseteq R \cap S$.

*   **הפרש סימטרי ($\Delta$) - ❌:**
    *   לכל $a \in A$, מתקיים $\langle a,a \rangle \in R$ וגם $\langle a,a \rangle \in S$.
    *   לפי הגדרת ה-XOR, הזוג $\langle a,a \rangle$ יוסר מהתוצאה.
    *   התוצאה היא יחס אירפלקסיבי.

---

# הסבר: סימטריות

*   **איחוד ($\cup$) - ✅:**
    *   אם $\langle a,b \rangle \in R \cup S$, אז $\langle a,b \rangle \in R$ או $\langle a,b \rangle \in S$.
    *   מסימטריות $R$ ו-$S$, נובע ש-$\langle b,a \rangle \in R$ או $\langle b,a \rangle \in S$.
    *   לכן $\langle b,a \rangle \in R \cup S$.

*   **חיתוך ($\cap$) - ✅:**
    *   אם $\langle a,b \rangle \in R \cap S$, אז $\langle a,b \rangle \in R$ וגם $\langle a,b \rangle \in S$.
    *   מסימטריות $R$ ו-$S$, נובע ש-$\langle b,a \rangle \in R$ וגם $\langle b,a \rangle \in S$.
    *   לכן $\langle b,a \rangle \in R \cap S$.

*   **הפרש סימטרי ($\Delta$) - ✅:**
    *   אם $\langle a,b \rangle \in R \Delta S$, הוא נמצא בדיוק באחד מהם (נניח ב-$R$ ולא ב-$S$).
    *   מסימטריות $R$, מתקיים $\langle b,a \rangle \in R$.
    *   מסימטריות $S$, אם $\langle b,a \rangle \in S$ אז $\langle a,b \rangle \in S$ (בסתירה).
    *   לכן $\langle b,a \rangle \in R \setminus S \subseteq R \Delta S$.

---

# הסבר: אנטי-סימטריות

*   **איחוד ($\cup$) - ❌:**
    *   הפרכה: נגדיר $R=\{\langle 1,2 \rangle\}$ ו-$S=\{\langle 2,1 \rangle\}$.
    *   שני היחסים אנטי-סימטריים (באופן ריק).
    *   באיחוד $R \cup S = \{\langle 1,2 \rangle, \langle 2,1 \rangle\}$ קיימים זוגות הפוכים עבור איברים שונים, לכן אינו אנטי-סימטרי.

*   **חיתוך ($\cap$) - ✅:**
    *   נניח ש-$R$ (או $S$) אנטי-סימטרי.
    *   מכיוון ש-$R \cap S \subseteq R$, כל זוג $\langle a,b \rangle, \langle b,a \rangle \in R \cap S$ נמצא גם ב-$R$.
    *   מאנטי-סימטריות $R$ נובע ש-$a=b$.

*   **הפרש סימטרי ($\Delta$) - ❌:**
    *   הפרכה: נגדיר $R=\{\langle 1,2 \rangle\}$ ו-$S=\{\langle 2,1 \rangle\}$.
    *   שניהם אנטי-סימטריים והחיתוך ביניהם ריק.
    *   ההפרש הסימטרי הוא $R \Delta S = \{\langle 1,2 \rangle, \langle 2,1 \rangle\}$, שאינו אנטי-סימטרי.

---

# הסבר: טרנזיטיביות

*   **איחוד ($\cup$) - ❌:**
    *   הפרכה: נגדיר $R=\{\langle 1,2 \rangle\}$ ו-$S=\{\langle 2,3 \rangle\}$.
    *   שני היחסים טרנזיטיביים (באופן ריק).
    *   באיחוד $R \cup S = \{\langle 1,2 \rangle, \langle 2,3 \rangle\}$ חסר הזוג $\langle 1,3 \rangle$, לכן אינו טרנזיטיבי.

*   **חיתוך ($\cap$) - ✅:**
    *   נניח ש-$\langle a,b \rangle, \langle b,c \rangle \in R \cap S$.
    *   מכאן שהזוגות נמצאים גם ב-$R$ וגם ב-$S$.
    *   מטרנזיטיביות $R$ נובע $\langle a,c \rangle \in R$, ומטרנזיטיביות $S$ נובע $\langle a,c \rangle \in S$.
    *   לכן $\langle a,c \rangle \in R \cap S$.

*   **הפרש סימטרי ($\Delta$) - ❌:**
    *   הפרכה: נגדיר $R=\{\langle 1,2 \rangle, \langle 2,3 \rangle, \langle 1,3 \rangle\}$ ו-$S=\{\langle 1,3 \rangle\}$.
    *   שני היחסים טרנזיטיביים.
    *   ההפרש הסימטרי הוא $R \Delta S = \{\langle 1,2 \rangle, \langle 2,3 \rangle\}$, שאינו טרנזיטיבי (חסר $\langle 1,3 \rangle$).

---

# יחסי שקילות וכיסוי

**שאלה:** נתונה הקבוצה $A$ ומשפחת תת-קבוצות $\mathcal{F} \subseteq \mathcal{P}(A)$.
נגדיר יחס $R$ על $A$ ע"י: 
$$R = \{\langle x,y \rangle \mid \exists S \in \mathcal{F} (x \in S \land y \in S) \}$$

**הוכיחו או הפריכו:**

א. $\bigcup \mathcal{F} = A$ $\Leftarrow$ $R$ רפלקסיבי.

ב. $R$ רפלקסיבי $\Leftarrow$ $\bigcup \mathcal{F} = A$.

ג. $(\forall S_1, S_2 \in \mathcal{F} (S_1 \neq S_2 \Rightarrow S_1 \cap S_2 = \emptyset))$ $\Leftarrow$ $R$ טרנזיטיבי.

ד. $R$ טרנזיטיבי $\Leftarrow$ $(\forall S_1, S_2 \in \mathcal{F} (S_1 \neq S_2 \Rightarrow S_1 \cap S_2 = \emptyset))$.

---

# פתרון: יחסי שקילות וכיסוי

<small>

*   **סעיף א' - ✅:**
    *   אם $R$ רפלקסיבי, לכל $x \in A$ מתקיים $\langle x,x \rangle \in R$.
    *   לפי הגדרת $R$, קיים $S \in \mathcal{F}$ כך ש-$x \in S$, ולכן $x \in \bigcup \mathcal{F}$.
    *   מכאן $A \subseteq \bigcup \mathcal{F}$, ובשילוב עם $\mathcal{F} \subseteq \mathcal{P}(A)$ נקבל שוויון.

*   **סעיף ב' - ✅:**
    *   אם $\bigcup \mathcal{F} = A$, לכל $x \in A$ קיים $S \in \mathcal{F}$ כך ש-$x \in S$.
    *   לכן $\langle x,x \rangle \in R$ לכל $x$, ו-$R$ רפלקסיבי.

*   **סעיף ג' - ❌:**
    *   הפרכה: $A=\{1,2,3\}$, $\mathcal{F}=\{\{1,2\}, \{1,2,3\}\}$.
    *   היחס $R$ הוא היחס המלא $A \times A$ (טרנזיטיבי), אך הקבוצות ב-$\mathcal{F}$ אינן זרות.

*   **סעיף ד' - ✅:**
    *   נניח $\langle x,y \rangle \in R$ ו-$\langle y,z \rangle \in R$. קיימות $S_1, S_2 \in \mathcal{F}$ כך ש-$x,y \in S_1$ ו-$y,z \in S_2$.
    *   מכיוון ש-$y \in S_1 \cap S_2$ והקבוצות ב-$\mathcal{F}$ זרות בזוגות, בהכרח $S_1 = S_2$.
    *   לכן $x,z \in S_1$, ומכאן $\langle x,z \rangle \in R$.

</small>





---

# שאלה: תכונות יחס על קבוצת החזקה

**שאלה:** תהי $A$ קבוצה בעלת יותר מאיבר אחד. יהי $R$ יחס על $\mathcal{P}(A)$ המוגדר באופן הבא:
$$R = \{\langle A_1, A_2 \rangle \mid A_2 \setminus A_1 \neq \emptyset \land A_1 \setminus A_2 \neq \emptyset\}$$

קבעו לגבי כל תכונה אם היא מתקיימת או לא:

א. $R$ רפלקסיבי.

ב. $R$ סימטרי.

ג. $R$ טרנזיטיבי.

ד. $R$ אנטי-סימטרי.

---

# פתרון: תכונות יחס על קבוצת החזקה

משמעות היחס: שני סעיפים קורים בו-זמנית: $A_2 \not\subseteq A_1$ וגם $A_1 \not\subseteq A_2$. כלומר, אין הכלה באף כיוון בין הקבוצות (הן "לא ניתנות להשוואה" ביחס ההכלה).

1.  **רפלקסיבי - לא:** לכל קבוצה $B$, $B \setminus B = \emptyset$, ולכן התנאי לא מתקיים. (היחס הוא אירפלקסיבי).
2.  **סימטרי - כן:** התנאי סימטרי לחלוטין עבור $A_1$ ו-$A_2$. אם $A_1$ לא מוכל ב-$A_2$ וגם $A_2$ לא מוכל ב-$A_1$, אז גם ההפך נכון.
3.  **טרנזיטיבי - לא:**
    *   הפרכה: $A_1 = \{1\}, A_2 = \{2\}, A_3 = \{1\}$ (כאשר $1,2 \in A$).
    *   מתקיים $A_1 R A_2$ (כי זרים ולא ריקים) ו-$A_2 R A_3$.
    *   אך לא מתקיים $A_1 R A_3$ כי $A_1 = A_3$ (אירפלקסיביות).
4.  **אנטי-סימטרי - לא:**
    *   הפרכה: עבור $A_1=\{1\}, A_2=\{2\}$, מתקיים $A_1 R A_2$ וגם $A_2 R A_1$.
    *   אך $A_1 \neq A_2$.

---
section: יחסי סדר (קס"ח)
---

# נושא 2: יחסי סדר (קס"ח)
**שאלה:** נתון הקס"ח $(\mathcal{P}(\{1,2\}), \subseteq)$.
1. ציירו את דיאגרמת הסה של הקס"ח.
2. מצאו את האיברים המינימליים, המקסימליים, המינימום והמקסימום.

**פתרון:**
הקבוצה היא $A = \{\emptyset, \{1\}, \{2\}, \{1,2\}\}$. היחס הוא הכלה.

1. **דיאגרמה:**
   *   $\{1,2\}$ למעלה.
   *   מתחתיו $\{1\}$ ו- $\{2\}$.
   *   למטה $\emptyset$.
   (יש קווים מ-$\emptyset$ לכולם, ומכולם ל-$\emptyset$, ומכל יחידון ל-$\emptyset$ ול-$\{1,2\}$).

2. **איברים:**
   *   **מינימום:** $\emptyset$ (מוכלת בכל קבוצה). לכן היא גם האיבר המינימלי היחיד.
   *   **מקסימום:** $\{1,2\}$ (מכילה את כל תת-הקבוצות). לכן היא גם המקסימלי היחיד.

**טיפ:** בקס"ח סופי, אם יש מינימום הוא יחיד ומינימלי.




---

# יחס סדר על המספרים

**שאלה:** נגדיר יחס $S$ על $\mathbb{Z} \setminus \{0\}$ כך:
$$x_1 S x_2 \iff 0 < \frac{x_1}{x_2} \le 1$$

**הוכיחו או הפריכו:**

1. הוכיחו כי $S$ יחס סדר (חלקי) על $\mathbb{Z} \setminus \{0\}$.

2. הוכיחו או הפריכו: $S$ יחס סדר קווי.
3. מצאו את קבוצת האיברים המינימליים וקבוצת האיברים המקסימליים ב-$S$.

---

# פתרון: יחס סדר על המספרים
<small>

1.  **יחס סדר:**
    *   **רפלקסיביות:** לכל $x \neq 0$, $\frac{x}{x} = 1$, ומתקיים $0 < 1 \le 1$.
    
    *   **אנטי-סימטריות:** נניח $x S y$ ו-$y S x$:
        *   מהתנאי $0 < \frac{x}{y}$ נובע כי ל-$x$ ו-$y$ יש אותו סימן.
        *   מהתנאים $\frac{x}{y} \le 1$ ו-$\frac{y}{x} \le 1$ נובע:
            *   אם $x, y > 0$: אז $x \le y$ ו-$y \le x$, לכן $x = y$.
            *   אם $x, y < 0$: אז $x \ge y$ ו-$y \ge x$, לכן $x = y$.

    *   **טרנזיטיביות:** אם $x S y$ ו-$y S z$, אז $0 < \frac{x}{y} \le 1$ ו-$0 < \frac{y}{z} \le 1$. מכפלתם נותנת $0 < \frac{x}{z} \le 1$, לכן $x S z$.

2.  **הפרכה (לא סדר קווי):** נבחר $x=1, y=-1$. מתקיים $\frac{1}{-1} = -1 \notin (0,1]$ וגם $\frac{-1}{1} = -1 \notin (0,1]$. לכן $1, -1$ אינם ניתנים להשוואה.

3.  **איברים:**
    *   **מינימליים:** $\{1, -1\}$. אם $x S 1$ אז $x$ חיובי ו-$x \le 1 \implies x=1$. אם $x S -1$ אז $x$ שלילי ו-$x \ge -1 \implies x=-1$.
    
    *   **מקסימליים:** אין. לכל $x > 0$ מתקיים $x S 2x$, ולכל $x < 0$ מתקיים $x S 2x$ (כי $\frac{x}{2x} = \frac{1}{2} \in (0,1]$).

</small>


---

# יחס סדר על פונקציות

**שאלה:** נגדיר $A = \{0, 1, 2\}^\mathbb{N}$. נגדיר יחס $R$ על $A$ כך:
$$fRg \iff (f=g) \lor (\forall n \in \mathbb{N}( f(n) = f(n)g(n)))$$ 

1. הוכיחו ש-$R$ יחס סדר על $A$.

2. מצאו את כל האיברים המקסימליים והאיברים המינימליים בקבוצה $A$ עם היחס $R$.

3. הוכיחו/הפריכו: היחס $R$ הוא יחס סדר קווי  על $A$. 




---

# פתרון: יחס סדר על פונקציות
<small>

1. **הוכחת יחס סדר:**
   *   **רפלקסיביות:** $f=f$ תמיד מתקיים, ולכן $fRf$.
   *   **אנטי-סימטריות:** נניח $fRg$ ו-$gRf$. אם $f \neq g$, אז לכל $n$, $f(n)=f(n)g(n)$ ו-$g(n)=g(n)f(n)$.
       *   אם $f(n)=2$, אז $2=2g(n) \implies g(n)=1$. ואז $1=1\cdot2=2$ סתירה. לכן $f(n) \neq 2$. כנ"ל $g(n) \neq 2$.
       *   אם $f(n)=1$, אז $g(n)=1$ וההיפך. אם $f(n)=0$, אז $g(n)=0$ וההיפך. לכן $f=g$.

   *   **טרנזיטיביות:** נניח $fRg, gRh$. נראה $fRh$.
       *   לכל $n$, אם $f(n)=0$, אז $f(n)=f(n)h(n)$ מתקיים ($0=0$).
       *   אם $f(n) \neq 0$, אז מ-$fRg$ נובע $g(n)=1$. מ-$gRh$ (ו-$g(n) \neq 0$) נובע $h(n)=1$.
       *   לכן $f(n)h(n) = f(n) \cdot 1 = f(n)$.
2. **איברים מינימליים ומקסימליים:**
   *   **מינימלי ומינימום:** פונקציית האפס $\mathbf{0}$ ($f(n)=0$ לכל $n$). לכל $g$, $0 = 0 \cdot g(n)$ מתקיים.
   *   **מקסימלי ומקסימום:** פונקציית האחד $\mathbf{1}$ ($f(n)=1$ לכל $n$). לכל $f$, $f(n) = f(n) \cdot 1$ מתקיים, לכן $f R \mathbf{1}$.
3. **הפרכה (לא קווי):**
   *   נבחר $f=(2, 0, 0, \dots)$ ו-$g=(0, 2, 0, \dots)$.
   *   $f(0)=2 \neq 2 \cdot g(0) = 0$, לכן לא מתקיים $fRg$.
   *   $g(0)=0$, אך $g(1)=2 \neq 2 \cdot f(1) = 0$, לכן לא מתקיים $gRf$.

</small>
---

# שרשראות

**הגדרה:** תת-קבוצה $A \subseteq \mathbb{R} \setminus \{0\}$ תקרא **מקורזלת** אם:
$$\forall x_1, x_2 \in A (\frac{x_1}{x_2} \in \mathbb{Q})$$

**שאלות:**

א. תהא $\mathcal{C}$ שרשרת של קבוצות מקורזלות ביחס להכלה. הוכיחו כי $\bigcup \mathcal{C}$ היא קבוצה מקורזלת.

ב. הוכיחו כי קיימת קבוצה מקורזלת $S$, כך שלכל $x \in \mathbb{R} \setminus S$ ($x \ne 0$) קיים $a \in S$ המקיים $\frac{x}{a} \notin \mathbb{Q}$.

<!-- ג. תהא $S$ קבוצה מקורזלת מקסימלית ביחס להכלה. מצאו את עוצמתה, $|S|$. קבעו האם העוצמה סופית, $\aleph_0$, $\aleph$, $2^{\aleph_0}$ או אחרת. -->




---
section: יחסי שקילות
layout: two-cols-header
---

# היחס הבונה את המספרים השלמים

**שאלה:** נגדיר יחס $R$ על $\mathbb{N} \times \mathbb{N}$ כך: 
$$\langle a,b \rangle R \langle c,d \rangle \iff a+d = b+c$$
הוכיחו כי זהו יחס שקילות. (שאלה זו בונה את המספרים השלמים $\mathbb{Z}$).


**פתרון:**

::left::

1. **רפלקסיביות:**
   * מחוק החילוף, לכל $\langle a,b \rangle \in \mathbb{N} \times \mathbb{N}$, מתקיים $a+b = b+a$.
   * לכן $\langle a,b \rangle R \langle a,b \rangle$.


2. **סימטריות:**
   * נניח $\langle a,b \rangle R \langle c,d \rangle$, כלומר $a+d = b+c$.
   * מהחלפת אגפים וחוק החילוף נקבל $c+b = d+a$.
   * לכן $\langle c,d \rangle R \langle a,b \rangle$.

::right::

3. **טרנזיטיביות:** נניח $\langle a,b \rangle R \langle c,d \rangle$ וגם $\langle c,d \rangle R \langle e,f \rangle$.
   *   משוואה 1: $a+d = b+c$
   *   משוואה 2: $c+f = d+e$
   *   נחבר את המשוואות: $(a+d) + (c+f) = (b+c) + (d+e)$.
   *   נצמצם $c$ ו-$d$ משני האגפים: $a+f = b+e$.
   *   מסקנה: $\langle a,b \rangle R \langle e,f \rangle$.

**לסיכום:** היחס הוא יחס שקילות.


---

# נושא 4: "מוגדר היטב" (Well Defined)
**שאלה:** נתבונן במרחב המנה $\mathbb{Z}_4 = \mathbb{Z}/4\mathbb{Z}$.
קבעו האם הפונקציה $f: \mathbb{Z}_4 \to \mathbb{Z}_4$ המוגדרת ע"י $f([x]) = [x^2 + 1]$ מוגדרת היטב.

**פתרון:**
כדי שפונקציה תהיה מוגדרת היטב על מרחב מנה, התוצאה לא צריכה להיות תלויה בנציג.
נניח $x \equiv y \pmod 4$. כלומר $x - y = 4k$ או $x = y + 4k$.
עלינו לבדוק האם $x^2 + 1 \equiv y^2 + 1 \pmod 4$.

נחשב:
$$x^2 + 1 = (y+4k)^2 + 1 = y^2 + 8ky + 16k^2 + 1$$
$$= (y^2 + 1) + 4(2ky + 4k^2)$$
ההפרש הוא כפולה של 4, ולכן $[x^2+1] = [y^2+1]$.
**מסקנה:** הפונקציה מוגדרת היטב.


---
section: פונקציות
---

# שאלה: תכונות של פונקציה על קבוצת החזקה

**שאלה:** תהי $A$ קבוצה לא ריקה כך ש-$|A| \ge 2$.

נגדיר פונקציה $f: A \times \mathcal{P}(A) \to \mathcal{P}(\mathcal{P}(A))$ ע"י:
$$f(x, U) = \{V \subseteq U \mid x \in V\}$$

1.  האם $f$ חח"ע? הוכיחו או הפריכו.

2.  האם $f$ על? הוכיחו או הפריכו.
3.  האם לכל $x \in A$ ולכל $U, W \in \mathcal{P}(A)$ מתקיים $f(x, U \cap W) = f(x, U) \cap f(x, W)$? הוכיחו או הפריכו.
4.  האם לכל $x \in A$ ולכל $U, W \in \mathcal{P}(A)$ מתקיים $f(x, U \cup W) = f(x, U) \cup f(x, W)$? הוכיחו או הפריכו.

---

# פתרון: תכונות של פונקציה

<small>

1.  **לא חח"ע:** נבחר $U_1 = \emptyset$ ו-$U_2 = \{y\}$ עבור $y \in A, y \ne x$.
    מכיוון ש-$x \notin U_1$ ו-$x \notin U_2$, נקבל $f(x, U_1) = \emptyset$ ו-$f(x, U_2) = \emptyset$, אך $(x, U_1) \ne (x, U_2)$.

2.  **לא על:**" כל איבר ב-$f(x, U)$ הוא קבוצה $V$ המקיימת $x \in V$.
    לכן, $f(x, U)$ לא יכולה להיות הקבוצה $\{\emptyset\}$ (כי $\emptyset$ אינה מכילה את $x$), והיא בקודומיין.

3.  **החיתוך נשמר (כן):**
    $$V \in f(x, U \cap W) \iff V \subseteq U \cap W \land x \in V$$
    $$\iff (V \subseteq U \land x \in V) \land (V \subseteq W \land x \in V)$$
    $$\iff V \in f(x, U) \land V \in f(x, W) \iff V \in f(x, U) \cap f(x, W)$$

4.  **האיחוד לא נשמר (לא):**
    דוגמה נגדית: $U=\{x\}, W=\{y\}$ (זרים). $U \cup W = \{x, y\}$.
    $V = \{x, y\} \subseteq U \cup W$ ומכיל את $x$, לכן $V \in f(x, U \cup W)$.
    אך $V \not\subseteq U$ (כי $y \in V$) ו-$V \not\subseteq W$ (כי $x \in V$), לכן $V$ לא נמצא באגף ימין.

</small>

---
section: תמונות וקדם-תמונות
layout: two-cols-header
---

# האם תמונת האיחוד שווה לאיחוד התמונות ?

**שאלה:** הוכיחו או הפריכו: לכל פונקציה $f: A \to B$ וקבוצות $X,Y \subseteq A$, מתקיים $f[X \cap Y] = f[X] \cap f[Y]$.

::left::

**פתרון (הפרכה):**
הטענה **אינה נכונה** באופן כללי
<br>   (היא נכונה רק אם $f$ חח"ע).

**דוגמה נגדית:**
*   $A = \{1, 2\}$, $B = \{a\}$.
*   נגדיר $f(1)=a, f(2)=a$.
*   $X = \{1\}, Y = \{2\}$.

::right::

**בדיקה:**
*   $X \cap Y = \emptyset$, ולכן $f[X \cap Y] = f[\emptyset] = \emptyset$.

*   מצד שני:
    *   $f[X] = \{a\}$
    *   $f[Y] = \{a\}$
    *   $f[X] \cap f[Y] = \{a\}$.
*   קיבלנו $\emptyset \neq \{a\}$.

**מסקנה:** שוויון לא מתקיים. 
<br> (זכרו: עבור קדם-תמונה $f^{-1}$, השוויון כן מתקיים לחיתוך).




---

# שאלה: שוויון פונקציות וקבוצות קטנות

**שאלה:** תהא $A$ קבוצה עם לפחות שלושה איברים ($|A| \ge 3$). 

נסמן ב-$S$ את קבוצת כל תתי-הקבוצות של $A$ בגודל 2:

$$S = \{X \subseteq A \mid |X| = 2\}$$

תהיינה $f,g: A \to A$ שתי פונקציות. הוכיחו את הטענות הבאות:

א. אם לכל $X \in S$ מתקיים $f[X] = g[X]$, אז $f=g$.

ב. אם לכל $X \in S$ מתקיים $f^{-1}[X] = g^{-1}[X]$, אז $f=g$.

---
layout: two-cols-header
---

# פתרון: שוויון פונקציות וקבוצות קטנות

::left::

<small>

**פתרון סעיף א':**
יהי $x \in A$. נראה כי $f(x) = g(x)$.
*   מכיוון ש-$|A| \ge 3$, קיימים $y, z \in A$ כך ש-$x, y, z$ שונים זה מזה.
*   נסתכל על $X_1 = \{x, y\}$ ו-$X_2 = \{x, z\}$. 
*   נתון $f[X_1] = g[X_1]$ ו-$f[X_2] = g[X_2]$.
*   מכאן ש-$\{f(x), f(y)\} = \{g(x), g(y)\}$ וגם $\{f(x), f(z)\} = \{g(x), g(z)\}$.
*   נניח בשלילה $f(x) \neq g(x)$.
*   מהשוויון הראשון נובע $f(x) = g(y)$ ו-$g(x) = f(y)$.
*   מהשוויון השני נובע $f(x) = g(z)$ ו-$g(x) = f(z)$.
*   מכאן ש-$g(y) = g(z)$. נסמן ערך זה ב-$a$.
*   עבור $X_3 = \{y, z\}$, מתקיים $f[X_3] = g[X_3]$.
*   אבל $g[X_3] = \{g(y), g(z)\} = \{a\}$, לכן $f[X_3] = \{f(y), f(z)\} = \{a\}$.
*   קיבלנו $f(y) = a$. אבל ראינו ש-$f(y) = g(x)$, לכן $g(x) = a = f(x)$, 
*   בסתירה להנחה.
*   **לסיכום:** $f(x) = g(x)$ לכל $x$, ולכן $f=g$.

</small>
::right::

<small>

**פתרון סעיף ב':**
יהי $x \in A$. נסמן $f(x) = a$. נניח בשלילה $g(x) = b$ כך ש-$a \neq b$.
*   מכיוון ש-$|A| \ge 3$, קיים $c \in A \setminus \{a, b\}$.

*   נסתכל על הקבוצה $X = \{a, c\} \in S$.
*   מתקיים $x \in f^{-1}[X]$ כי $f(x) = a \in X$.
*   לפי הנתון $f^{-1}[X] = g^{-1}[X]$, לכן $x \in g^{-1}[X]$.
*   מכאן ש-$g(x) \in \{a, c\}$. בסתירה לכך ש-$g(x) = b$ ו-$b \notin \{a, c\}$.
*   **לסיכום:** $f(x) = g(x)$ לכל $x$, ולכן $f=g$.

</small>

---
section: אינדוקציה
layout: two-cols-header
---

# גודל קבוצת החזקה
**שאלה:** הוכיחו כי לכל קבוצה סופית $A$ בגודל $n$, גודל קבוצת החזקה הוא $|\mathcal{P}(A)| = 2^n$.

**הוכחה:**
*   **בסיס ($n=0$):** $A=\emptyset$, $\mathcal{P}(A)=\{\emptyset\}$. הגודל הוא $1 = 2^0$.

*   **צעד ($n \to n+1$):** נניח נכון ל-$n$. תהי $B$ בגודל $n+1$. נבחר איבר $b \in B$.
*   נגדיר $A = B \setminus \{b\}$ (בגודל $n$).
*   נחלק את תת-הקבוצות של $B$ לשני סוגים:
    1.  קבוצות שאינן מכילות את $b$: אלו בדיוק תת-הקבוצות של $A$. לפי הנחה יש $2^n$ כאלו.

    2.  קבוצות שמכילות את $b$: כל קבוצה כזו היא מהצורה $X \cup \{b\}$ כאשר $X \subseteq A$. יש בדיוק $2^n$ כאלו.
*   סה"כ: $2^n + 2^n = 2 \cdot 2^n = 2^{n+1}$.



